// const assert = require('assert');
// const cookieAuth = require('../middlewares/authenticated').cookieAuth;

// describe('cookie authorization middleware', function() {
//     it('Should add a `auth` to the `req` parameter', function() {
//         const req = res = {}
//         cookieAuth(req, res, () => {});

//         assert.ok(req.cookies.auth == 'value');
//     });
// });